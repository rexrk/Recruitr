spring.application.name=recruitr
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level: %logger{36} - %msg%n

# sql config
spring.datasource.url=jdbc:mysql://192.168.49.2:30306/recruitr
spring.datasource.username=raman
spring.datasource.password=admin

# JPA config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# App user Admin Credentials
app.admin.username=raman
app.admin.password=admin

#actuator endpoints
management.endpoints.web.exposure.include=info,health,prometheus,refresh
management.endpoint.health.show-details=always